package spring.mall.web.session.view;

public class LoginRequest extends BaseSessionRequest {
    public LoginRequest(String username, String password) {
        super(username, password);
    }
}
